services:
  - type: web
    name: dashboard-ondas-calor
    # Usa Docker ao invés de buildpack Python
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: PORT
        value: 8050
      - key: PYTHONUNBUFFERED
        value: 1

